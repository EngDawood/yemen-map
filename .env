# Base map tiles published by .github/workflows/basemap.yml.
# Leave empty to run without a base map. If the files are unreachable the map loads without it.
VITE_BASEMAP_URL=https://engdawood.github.io/yemen-map
