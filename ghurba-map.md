# خريطة الغربة · Ghurba Map

> وضع جديد داخل مشروع `yemen-map`: كل يمني في الخارج يرسم خطًا من منطقته في اليمن إلى المدينة التي يعيش فيها. مع كل مشاركة تمتلئ الكرة الأرضية بخطوط مضيئة تعود كلها إلى اليمن.

## 1. الفكرة

الغربة جزء من حياة معظم البيوت اليمنية، لكنها غير مرئية. هذه الخريطة تجعلها مرئية: خط لكل شخص، من مديريته إلى مدينته. لا أسماء ولا صور، فقط خطوط وأرقام تكفي لتقول: "أهل حجة في الرياض 214، وأهل تعز في برمنغهام 37".

**لماذا ستنتشر:**
- كل مشارك يرى خطه على الخريطة فورًا، ويحصل على بطاقة جاهزة للمشاركة.
- المقارنة بين المحافظات والمدن تفتح نقاشًا في المجموعات.
- المحتوى من الناس أنفسهم، فالخريطة تكبر مع كل مشاركة.

## 2. تجربة الزائر

1. **الدخول:** زر في الأعلى يبدّل بين "اليمن" و"الغربة". في وضع الغربة تتحول الخريطة إلى كرة أرضية تدور ببطء، والأقواس تتوهج من اليمن إلى العالم.
2. **العدّاد:** في اللوحة الجانبية: "12,430 يمني في 87 دولة و540 مدينة".
3. **أضف خطك:**
   - من أين أنت؟ محافظة ثم مديرية (من `yemen.json` الموجود).
   - أين تعيش الآن؟ بحث عن مدينة بالعربية أو الإنجليزية.
   - تحقق Turnstile، ثم إرسال.
4. **اللحظة المهمة:** يُرسم خطه بحركة من مديريته إلى مدينته، وتظهر جملة:
   "أنت واحد من **214** من **حجة** في **الرياض**".
5. **البطاقة:** صورة مربعة فيها الخط والجملة ورابط الموقع، مع زر مشاركة (واتساب، ستوري، تحميل).

**الاستكشاف:**
- الضغط على محافظة: "أين أهل تعز في العالم؟" تظهر خطوطها فقط، مع قائمة أكثر المدن.
- الضغط على مدينة: "من أين يمنيّو جدة؟" تظهر المحافظات التي جاؤوا منها.

## 3. الخصوصية (غير قابلة للتفاوض)

- **لا أسماء، لا بريد، لا حسابات.**
- الأصل على مستوى **المديرية** فقط، والوجهة على مستوى **المدينة** فقط (مركز المدينة وليس موقع الشخص).
- لا نطلب الموقع الجغرافي من المتصفح أبدًا.
- الواجهة لا تستقبل صفوفًا فردية، بل أرقامًا مجمّعة فقط (مديرية، مدينة، عدد).
- عنوان IP لا يُخزَّن، فقط بصمة مجزّأة (hash) تُستخدم للحد من التكرار، وتُحذف بعد 30 يومًا.

السبب: كثير من اليمنيين في الخارج في أوضاع إقامة حساسة. الخريطة يجب أن تكون آمنة لهم قبل أن تكون جميلة.

## 4. التقنية

الموقع الحالي ثابت بالكامل. خريطة الغربة تحتاج جزءًا صغيرًا من الخادم، ولهذا **يُثبَّت المشروع على Cloudflare** (Vercel لا يدعم D1).

| الجزء | الاختيار | ملاحظة |
| --- | --- | --- |
| الكرة الأرضية | `map.setProjection({ type: 'globe' })` | مدعوم في MapLibre 5 الموجود في المشروع |
| الأقواس | خطوط great-circle عبر `@turf/great-circle` في طبقة `line` عادية | أبسط من deck.gl ولا تضيف مكتبة ثقيلة |
| الواجهة الخلفية | Cloudflare Pages Functions (`functions/api/*`) | بنفس المستودع |
| قاعدة البيانات | Cloudflare D1 | |
| الحماية من السبام | Cloudflare Turnstile | |
| المدن | ملف ثابت `public/data/cities.json` | حوالي 1000 مدينة من GeoNames (CC BY) مع أسماء عربية |
| البطاقة | Canvas في المتصفح + Web Share API | بلا خادم |

### تعديلات على الكود الحالي

- `src/map.js`: قيد `maxBounds` الحالي (`[34,7]` إلى `[62,24]`) يمنع رؤية العالم. يُزال في وضع الغربة ويعود عند الرجوع لوضع اليمن.
- `src/main.js`: إضافة `state.mode = 'yemen' | 'ghurba'` ودعم الرابط `?view=ghurba`.
- نقطة الأصل لكل مديرية تُؤخذ من `district-labels.geojson` الموجود.
- `src/i18n.js`: نصوص الوضع الجديد بالعربية والإنجليزية.

### قاعدة البيانات

```sql
CREATE TABLE lines (
  id          INTEGER PRIMARY KEY,
  district_id TEXT NOT NULL,      -- من yemen.json
  city_id     TEXT NOT NULL,      -- من cities.json
  created_at  INTEGER NOT NULL,
  ip_hash     TEXT NOT NULL,
  status      TEXT NOT NULL DEFAULT 'ok'   -- ok | hidden
);
CREATE INDEX idx_pair ON lines (district_id, city_id);
```

### الواجهة البرمجية

| المسار | العمل |
| --- | --- |
| `GET /api/lines` | يعيد `[{ d, c, n }]` مجمّعة. تُخزَّن مؤقتًا على الحافة 60 ثانية |
| `POST /api/lines` | يستقبل `{ district_id, city_id, turnstile_token }`، يتحقق، ثم يعيد رقم "أنت واحد من N" |

**قواعد الإرسال:** التحقق أن المديرية والمدينة موجودتان في القوائم الثابتة، ومشاركة واحدة لكل بصمة يوميًا، وعلامة في `localStorage` تخفي زر الإضافة بعد المشاركة.

## 5. الأداء

- مع آلاف الأزواج (مديرية، مدينة) يبقى عدد الخطوط معقولًا لأن كل زوج يُرسم مرة واحدة، وسُمك الخط يعكس العدد.
- حساب مسارات great-circle يتم مرة واحدة عند التحميل.
- على الهواتف الضعيفة: إيقاف الدوران التلقائي وتقليل التوهج.

## 6. الإطلاق

**مشكلة البداية الفارغة:** خريطة بلا خطوط لا يشارك فيها أحد.

1. **الإطلاق الهادئ:** الرابط يُرسل للمعارف والمجموعات القريبة فقط، والهدف 100 خط.
2. **الإطلاق العام:** منشور بفيديو قصير للكرة وهي تدور بالخطوط، مع جملة واحدة: "ارسم خطك".
3. **موجات لاحقة:** كل رقم لافت يصبح منشورًا ("أهل إب في 40 دولة").

## 7. المراحل

**النسخة الأولى (أسبوع تقريبًا):**
- وضع الغربة، الكرة الأرضية، الأقواس، العدّاد.
- نموذج الإضافة مع Turnstile.
- جملة "أنت واحد من N" والبطاقة.

**النسخة الثانية:**
- الاستكشاف حسب المحافظة وحسب المدينة.
- رسالة قصيرة اختيارية ("اشتقت لمطر إب") تمر بمراجعة يدوية قبل النشر.
- لوحة إدارة بسيطة لإخفاء المشاركات المسيئة.

**خارج النطاق عمدًا:** الحسابات، التعليقات، الصور، المواقع الدقيقة.

## 8. أسئلة مفتوحة

- هل نعرض الخطوط التي عددها 1 أو 2؟ عرضها يشجع المشاركة، وإخفاؤها يزيد الخصوصية في المدن الصغيرة. الاقتراح: عرضها بلا رقم ظاهر.
- هل نسمح بالمشاركة لمن هم داخل اليمن (نازحون بين المحافظات)؟ فكرة قوية لكنها موضوع مختلف، والأفضل تأجيلها.
